# استخدم صورة Node.js الرسمية
FROM node:20

# إنشاء مجلد داخل الحاوية
WORKDIR /app

# نسخ ملفات المشروع إلى الحاوية
COPY . .

# تثبيت الاعتماديات (قد تحتاج --legacy-peer-deps في بعض الحالات)
RUN npm install --legacy-peer-deps

# تعيين البورت الذي سيعمل عليه التطبيق
EXPOSE 3000

# أمر تشغيل التطبيق (عدّله حسب الملف الرئيسي لديك)
CMD ["npm", "start"]
